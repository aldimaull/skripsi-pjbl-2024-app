import React from "react";
import CodeEditor from "@/components/CodeEditor";

const page = () => {
  return (
    <div>
      <CodeEditor defaultValue="// comment" />
    </div>
  );
};

export default page;
